steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-f', 'apps/judge/Dockerfile', '-t', '${_IMAGE}', '.']
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', '${_IMAGE}']
images: ['${_IMAGE}']
